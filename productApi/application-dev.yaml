spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:productdb
    username: sa
    password:

  h2:
    console:
      enabled: true
      path: /h2-console

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  data:
    redis:
      host: redis-19217.c251.east-us-mz.azure.cloud.redislabs.com
      port: 19217
      username: default
      password: bRWruk3Nnzpl9NKpSA62pTqagiRuqgcj

  cache:
    type: redis

  mongodb:
    uri: mongodb+srv://romeropajuelorenzo_db_user:wa4cqdoiAHfKtTBI@cluster0.kdxrkh9.mongodb.net/?appName=Cluster0
    database: ProductDB-Auditoria

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    instance-id: ${spring.application.name}:${random.uuid}
    prefer-ip-address: true

app:
  logMongo: true

server:
  port: 0
